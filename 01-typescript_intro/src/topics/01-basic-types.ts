const name: string = 'Gerson';      //let para variables que cambiaran y const para variable que no cambiar
let hpPoints: number | string = 95;  // El simbolo | e utiliza para darle doble  tipo de dato.
const isAlive: boolean = true; //


hpPoints = 'FULL';

console.log({
    name, hpPoints, isAlive
});
export {};